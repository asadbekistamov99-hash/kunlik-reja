<?xml version="1.0" encoding="utf-8"?>
<!-- Jarvis Ultra logo scaled into the adaptive-icon viewport (108dp canvas, ~72dp visible).
     A 16dp inset keeps the logo's own outer glow just outside the launcher mask. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/jarvis_logo"
    android:inset="16dp" />
