<?xml version="1.0" encoding="utf-8"?>
<!-- Cloud auto-backup is disabled: the database is encrypted with a device-bound Keystore key.
     Use Settings -> Zaxiralash for portable, password-protected backups. -->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
